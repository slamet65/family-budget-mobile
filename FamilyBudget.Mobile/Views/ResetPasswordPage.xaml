<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:FamilyBudget.Mobile.ViewModels"
             x:DataType="vm:ResetPasswordViewModel"
             x:Class="FamilyBudget.Mobile.Views.ResetPasswordPage"
             Title="Reset password"
             BackgroundColor="{StaticResource Surface}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">

            <Label Text="{Binding UserName, StringFormat='Set a new password for {0}'}"
                   FontSize="16" TextColor="{StaticResource OnSurface}" />

            <VerticalStackLayout Spacing="4">
                <Label Text="New password (min. 8 characters)" FontSize="12" TextColor="{StaticResource OnSurfaceVariant}" />
                <Entry Text="{Binding NewPassword}" IsPassword="True" Placeholder="New password" />
            </VerticalStackLayout>

            <Button Text="Reset password"
                    Command="{Binding SaveCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"
                    Margin="0,10,0,0" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
