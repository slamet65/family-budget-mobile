<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- networkSecurityConfig only allows cleartext for 10.0.2.2 (the emulator's alias for the
	     dev machine running `wrangler dev`), scoped to that one non-routable dev address, so
	     it's inert in production and safe to leave referenced in Release builds too. -->
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true" android:networkSecurityConfig="@xml/network_security_config"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
</manifest>